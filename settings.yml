prevent_text_selection:
  default: true
  client: true
  type: bool
  description: "Enable to prevent text selection"

prevent_right_click:
  default: true
  client: true
  type: bool
  description: "Enable to prevent right-click actions"

alert_enabled:
  default: false
  client: true
  type: bool
  description: "Enable or disable the alert message"

alert_message:
  default: "คำเตือน: การคลิกขวาถูกปิดใช้งานในเว็บไซต์นี้"
  client: true
  type: string
  description: "Alert message shown when right-click is disabled"

custom_settings:
  copyright_notice_text:
    default: "คำเตือน: หัวข้อนี้มีเนื้อหาที่อาจมีลิขสิทธิ์ กรุณาอย่าทำการคัดลอกหรือเผยแพร่โดยไม่ได้รับอนุญาต"
    client: true
    type: text
  copyright_notice_category_id:
    default: 1  # แทนที่ด้วย ID ของหมวดหมู่ที่ต้องการ
    client: true
    type: integer